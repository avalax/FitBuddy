<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="de.avalax.fitbuddy"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <application
        android:name="de.avalax.fitbuddy.presentation.FitbuddyApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name="de.avalax.fitbuddy.presentation.workout.WorkoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="de.avalax.fitbuddy.presentation.edit.workout.EditWorkoutActivity"/>
        <activity android:name="de.avalax.fitbuddy.presentation.edit.exercise.EditExerciseActivity"/>
        <activity android:name=".presentation.summary.FinishedWorkoutActivity"/>
    </application>

</manifest>